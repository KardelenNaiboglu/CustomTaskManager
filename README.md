# Custom Task System
This system simplifies the management of asynchronous tasks in Unity by processing them sequentially. It ensures that only one task runs at a time, making it easy to handle tasks in a predefined order.
